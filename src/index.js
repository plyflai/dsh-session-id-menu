/** Host half of dsh-session-id-menu. */

/** Stable Cordis plugin name. */
export const name = 'dsh-session-id-menu'

/** The behavior is implemented by the browser half. */
export function apply() {}
